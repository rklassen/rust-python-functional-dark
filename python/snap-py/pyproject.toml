[project]
name = "snap-py"
version = "0.2.0"
description = "Python export surface for the Snap graph serialization format."
requires-python = ">=3.12"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["snap_py"]
